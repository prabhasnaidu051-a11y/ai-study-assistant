stages:
  - test

python_checks:
  stage: test

  image: python:3.12

  before_script:
    - pip install -r backend/requirements.txt
    - pip install ruff mypy bandit

  script:
    - ruff check .
    - mypy backend
    - bandit -r backend
